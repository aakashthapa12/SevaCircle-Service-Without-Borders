# Backend Environment Configuration Template
# File: backend/.env
# 
# Copy this content to backend/.env and customize as needed

# ===== DATABASE CONFIGURATION =====
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# Default (local PostgreSQL): postgresql://postgres:postgres@localhost:5432/local_services_db
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/local_services_db"

# Alternative options:
# - Cloud (Supabase): postgresql://user:password@db.supabase.co:5432/postgres
# - Cloud (Azure): postgresql://user:password@servername.postgres.database.azure.com:5432/dbname
# - SQLite (local): file:./local.db
# Leave as above for local development

# ===== APPLICATION CONFIGURATION =====
# Node environment
# Options: development | production | test
NODE_ENV="development"

# Server port where NestJS API will run
# Change this if port 3001 is already in use
PORT=3001

# ===== OPTIONAL SETTINGS =====
# JWT Secret (for authentication - add when implementing auth)
# JWT_SECRET="your-secret-key-here-change-in-production"

# CORS Origins (allowed frontend URLs)
# Currently set in main.ts, can be overridden here if needed
# CORS_ORIGINS="http://localhost:3000,http://localhost:3001"

# Logging Level
# Options: debug | log | warn | error | fatal
# LOG_LEVEL="log"

# ===== NOTES =====
# 1. DATABASE is OPTIONAL for hackathon demo
#    - Backend will work fine without PostgreSQL
#    - Database errors are logged but won't crash the server
#
# 2. For production, change:
#    - NODE_ENV to "production"
#    - DATABASE_URL to your production database
#    - Secure your JWT_SECRET
#
# 3. Never commit sensitive data!
#    - This file should be in .gitignore
#    - Use environment variable services for production
